"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var dep;
try {
    dep = require("uws");
}
catch (e) {
    dep = require("ws");
    // display warning message only once (master process)
    if (!process.send) {
        console.warn("'uws' not installed. using 'ws' instead.");
    }
}
exports.WebSocketServer = dep.Server;
exports.default = dep;
