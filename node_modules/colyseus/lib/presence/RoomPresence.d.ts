import { RoomPresence } from './Presence';
import { RedisPresence } from './RedisPresence';
export declare class DefaultRoomPresence implements RoomPresence {
    rooms: {
        [name: string]: {
            [roomId: string]: number;
        };
    };
    adapter: RedisPresence;
    registerRoom(roomName: string, roomId: string, pid: number): void;
    unregisterRoom(roomName: string, roomId: string, pid: number): void;
}
